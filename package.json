{
  "name": "remote-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/menu-bar.js",
  "author": "dudu",
  "description": "A local mobile remote for Mac media playback.",
  "scripts": {
    "app": "electron .",
    "package:mac": "electron-builder --mac dir -c.mac.identity=- -c.mac.notarize=false",
    "dist:mac": "node scripts/with-env.js node scripts/verify-mac-release-env.js && node scripts/with-env.js electron-builder --mac dmg zip --universal && node scripts/with-env.js node scripts/verify-mac-release.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/menu-bar.js && node --check scripts/with-env.js && node --check scripts/verify-mac-release-env.js && node --check scripts/verify-mac-release.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "electron": "^41.2.1",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.dudu.macremote",
    "productName": "Mac Remote",
    "artifactName": "${productName}-${version}-${arch}.${ext}",
    "directories": {
      "output": "dist"
    },
    "mac": {
      "category": "public.app-category.utilities",
      "icon": "build/icon.icns",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "target": [
        "dmg",
        "zip"
      ],
      "extendInfo": {
        "LSUIElement": true,
        "NSAppleEventsUsageDescription": "Mac Remote sends keyboard events through System Events so your phone can control focused media playback.",
        "NSLocalNetworkUsageDescription": "Mac Remote serves a local pairing page so your phone can control this Mac on the same network."
      }
    }
  }
}
